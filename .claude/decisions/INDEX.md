# Décisions d'architecture — shaderlab

Une ligne par ADR. `[statut]` reprend le `status:` du frontmatter : un ADR
`superseded` ne doit pas être lu comme une contrainte active.

- [ADR-0001](ADR-0001-densite-ui-controles-repetes.md) [active] — un contrôle répété par ligne devient un contrôle unique dans une zone fixe (en-tête OU pied, amendement 2026-07-28) agissant sur la sélection ; s'applique à tout élément ajouté, au moment où il est ajouté (checklist en 5 points)
- [ADR-0002](ADR-0002-abandon-round-trip-lightroom.md) [active] — le round-trip Lightroom est abandonné, shaderlab devient un éditeur autonome ; libère le statut spécial du document de fond et débloque « toutes les images sont des calques »
- [ADR-0003](ADR-0003-sens-affichage-pile-calques.md) [superseded by ADR-0004] — la liste des calques est le miroir du tableau (sens Photoshop) ; renversé le lendemain, ne plus lire comme une contrainte active
- [ADR-0004](ADR-0004-sens-causal-pile-calques.md) [active] — la liste se lit dans l'ordre du TRAITEMENT (photo d'abord, puis les effets qui la traitent) ; affichage seul, modèle inchangé, conversion dans le même module pur unique, désormais l'identité
- [ADR-0005](ADR-0005-rattachement-par-proximite.md) [active] — un effet appartient à la photo qui le PRÉCÈDE dans la chaîne (le fond du document compris, passé par son id conventionnel) ; ~~l'écrêtage garde la priorité~~ (point 2 CADUC depuis ADR-0020 : l'écrêtage est retiré, la proximité est seule) ; la lisibilité du groupe l'emporte sur l'exactitude littérale, abandonnée le 2026-07-29
- [ADR-0006](ADR-0006-fond-export-blanc.md) [active] — le fond d'un export est BLANC, plus noir (« lecture planche contact ») ; le damier reste inexprimable à l'export par construction ; deux références de rendu régénérées, sept identiques à l'octet
- [ADR-0007](ADR-0007-format-de-toile-a-la-creation.md) [active] — le format de la toile se choisit à la création, la taille de la photo restant le MÊME chemin de code par défaut ; formats relatifs dérivés par contenance et orientés comme la photo, A3 absolu à 300 dpi ; dimension du document issue d'une source unique ; borne `MAX_CANVAS_PIXELS = 64 Mpx` calibrée sur les mesures VRAM
- [ADR-0008](ADR-0008-effet-jamais-sur-un-calque-photo.md) [active] — un effet ne se pose jamais sur un calque photo
- [ADR-0009](ADR-0009-deplacement-libre-du-viewport.md) [active] — le déplacement de la vue est libre à tous les zooms (`MIN_VISIBLE_FRACTION = 0.25`), le régime de centrage de `clampOffset` est retiré et le centrage descend dans `fitViewport` ; le MUST du PRD (« l'image ne sort jamais entièrement ») est tenu, pas renversé
- [ADR-0010](ADR-0010-le-gaussien-reste-hors-du-registre.md) [active] — le flou gaussien (et ses dégénérescences box/average) n'entre pas au registre : la référence dit qu'il lave l'image, un flou doux reste atteignable par `lensBlur` à bokeh nul ; ⚠️ sa troisième conséquence (« le lissage sans perte de contour a son propre effet ») est CADUQUE depuis ADR-0011, le refus du gaussien ne l'est pas
- [ADR-0011](ADR-0011-retrait-de-surface-blur.md) [active] — `surfaceBlur` sort du registre sur verdict d'usage (« tous les effets de flou sont un peu inutiles à part le motion blur ») ; trois paramètres, donc rien à refondre ; le garde d'ADR-0010 a déménagé vers `registry.test.ts` AVANT la suppression, sinon on supprimait la décision avec l'effet
- [ADR-0012](ADR-0012-retrait-de-posterize.md) [active] — `posterize` sort du registre, couvert par `dither` ; la couverture a été PROUVÉE avant le retrait (scénario sérigraphie porté mot pour mot, mêmes 32 valeurs distinctes) et non affirmée ; les défauts des deux divergent volontairement (axe linéaire vs perceptuel)
- [ADR-0013](ADR-0013-outlines-absorbe-colored-edges.md) [active] — `outlines` absorbe `coloredEdges` en mode d'encre (Encre unique / Roue d'orientation) ; les neuf premiers index restent ceux d'`outlines` (persistés dans les presets), et la fusion est PROUVÉE sans perte — les deux références portées ressortent identiques à l'octet ; la fusion d'`echoOutlines` attend des passes conditionnelles dans le pipeline
- [ADR-0017](ADR-0017-lens-flare-rouvre-la-famille-des-halos.md) [active] — `lensFlare` (fantômes + anneau + voile ; aigrettes écartées) entre au registre dans la famille des HALOS, close à deux le matin même : un flare n'est pas une déformation, c'est de la lumière AJOUTÉE, donc il répond à « que renvoie l'objectif » et non à « que déforme sa forme ». Source posée ET automatique en UNE machinerie — le point posé est injecté dans le bright-pass, d'où des fantômes qui héritent gratuitement de la forme du diaphragme ; `aperture_radius` extrait vers `effects/aperture.ts` (un objectif n'a qu'un diaphragme). ⚠️ Premier effet où l'avertissement d'`EffectPass.enabled` mord : la composite porte le même prédicat que les passes, sinon elle ajoute l'image à elle-même
- [ADR-0016](ADR-0016-lens-distortion-absorbe-chromatic-bleed.md) [active] — `lensDistortion` absorbe `chromaticBleed`, et c'est une MESURE qui a instruit le dossier : les deux effets s'écartaient de 0,005 % des canaux sur le cas radial (doublon réel), mais l'orientation du décalage valait 23,1 % et n'existait nulle part ailleurs — franges tangentielles d'un objectif décentré. `aberrationAngle` a donc été porté AVANT le retrait, un paramètre pour tout un effet ; le défaut prend une BRANCHE d'échelle pour garder la référence existante au bit ; ⚠️ seule absorption NON identique à l'octet (implémentations indépendantes), trois références régénérées dont `photo-double-exposure` (3 octets d'écart)
- [ADR-0015](ADR-0015-outlines-absorbe-echo-outlines.md) [active] — `outlines` absorbe `echoOutlines` en TROISIÈME mode de détection (Échos de la forme) ; la fusion était décidée depuis ADR-0013 et bloquée sur `EffectPass.enabled`, arrivé le même jour — les neuf passes de pyramide portent un prédicat et ne coûtent rien aux deux modes locaux, une seule variante de shader pour les trois ; référence portée identique à l'octet (`git mv` détecte le renommage à 100 %) ; `MAX_EFFECT_PARAMS` 24 → 32 ; la question de NOM ouverte par le cahier s'éteint d'elle-même ; ⚠️ au passage, `outlines.threshold` était plafonné à 0,6 alors que deux références verrouillaient 0,8 — rendu inaccessible depuis le curseur, corrigé ici
- [ADR-0014](ADR-0014-lens-distortion-absorbe-anamorphic-streak.md) [active] — `lensDistortion` est écrit (fisheye signé + trois modes d'aberration dont la longitudinale, qui défocalise au lieu de déplacer) et ABSORBE `anamorphicStreak` : une traînée sur un seul axe est ce que fait un verre cylindrique, pas un halo ; ses quatre passes sont conditionnelles et son intensité vaut 0 par défaut, donc un fisheye seul n'alloue rien ; ⚠️ recouvrement assumé avec `chromaticBleed`, désormais candidat au retrait
- [ADR-0020](ADR-0020-retrait-de-l-ecretage.md) [active] — l'ÉCRÊTAGE sort du dépôt, sans remplaçant (Antoine 2026-08-21, portée « retrait sec ») : le libellé « Écrêter sur la photo du dessus » contredisait le modèle (« le calque photo situé EN DESSOUS »), les deux vrais dans leur repère depuis qu'ADR-0004 a inversé l'affichage — ET le geste ne servait pas. Retrait mesuré AVANT d'écrire : aucune référence de pixels, aucun preset (`capture` ne prenait pas le champ), et la PROXIMITÉ d'ADR-0005 reprend seule le regroupement (elle était déjà le cas général). Partent `clipToBelow`, `layers/clipping.ts`, `setLayerClip`, l'exception d'isolation, `clipToCoverage`/`clipCoverageView`, la case et la flèche coudée ; garde de retrait dans `layerStack.test.ts`. ⚠️ CE QUI EST PERDU : borner un effet à la COUVERTURE d'une photo, sans aucun équivalent — les sources de masque sont une union fermée `gradient · luminosity · colorRange`, aucune géométrique. À retrouver par la sélection géométrique du ticket 11, jamais par une case. Le binding 6 et le chemin `hasImageSource` (double exposure) sont intacts ; ADR-0008 n'est PAS touché
- [ADR-0019](ADR-0019-retrait-de-emboss.md) [active] — `emboss` (le Relief) sort du registre sur verdict d'usage (« relief est horrible », Antoine 2026-08-21) : gaufrage gris directionnel = filtre « Photoshop 2005 » proscrit, sa propriété (direction du bord) déjà mieux couverte par `outlines` ; `edgeGradient.ts` redevient à un seul lecteur, garde de retrait dans `registry.test.ts`, registre 27 → 26 ; retrait indépendant du revert de `noise` le même jour
- [ADR-0021](ADR-0021-retrait-des-matieres-pave.md) [active] — les CINQ MATIÈRES DE PAVÉ sortent de `glass`, retrait sec (Antoine 2026-08-27, « Pavé nuage est horrible ») : quatrième refus daté, après trois corrections livrées entre les refus du 2026-08-13 (joint adouci, granulométrie, variation par bloc) qui n'ont pas retourné le verdict. Partent les 5 matières, leurs 8 paramètres, la section « Pavé » (seule de l'effet à porter une `appliesWhen`), `verre_hauteurPave`/`verre_mosaiquePave` et les 3 branches WGSL, 5 scénarios + 5 références + 5 entrées ATTENDU, 8 déclarations d'applicabilité. ⚠️ Les 5 matières et les 8 paramètres étaient les DERNIERS de leurs listes, donc AUCUN index ne bouge et les 13 références de la feuille rendent le même bit — c'est le gate discriminant. `glass` : 22 → 14 params, 14 → 9 matières, 3 → 2 sections ; dépôt 124 → 119 PNG ; le REGISTRE reste à 26 (ce n'est pas un effet qui part). ⚠️ CE QUI EST PERDU : le seul verre À CELLULES, sans équivalent — aucune autre matière ne sépare l'image en régions, aucun effet ne peint une trame, et les sources de masque n'ont rien de géométrique. ⚠️ Un preset citant un pavé ne retombe PAS sur une matière voisine : il n'y a aucun clamp sur le chemin, la valeur sort par le bloc des cannelures en feuille PLANE. `sourceMipmaps` RESTE (le retirer changerait le rendu de la feuille). Le chantier esthétique continue sur les feuilles (tickets 17-19, diffusion, contraste)
- [ADR-0018](ADR-0018-la-texture-est-un-effet-pas-un-calque-photo.md) [active] — une texture est un EFFET (`texture`, 22e du registre), pas un calque photo : le paramètre porte le RANG dans le catalogue trié, le binding 7 porte les pixels (`textureLibraryStore`), ce qui contourne `params: Record<string, number>` — le mur qui bloque aussi la typographie. Renverse D1 du design du même jour, sur trois relances d objection d Antoine. ⚠️ L argument donné d abord contre l effet (« ADR-0008 l interdirait ») était FAUX : cet ADR interdit un `effectId` SUR un calque photo, pas un calque d effet écrêté au-dessus. L effet SORT le scan brut — `blendMode`/`opacity` du calque font le mélange, d où onze modes au lieu de quatre. `EffectModule` gagne `defaultBlendMode`/`defaultOpacity` ; `libraryTexture` + passes internes est refusé (exclut `lensFlare`) ; `exportFrame` attend les textures en vol ; l encre suit en TRANSVERSAL (`effects/inkTexture.ts`, adopté par halftone/dither/hatching)
